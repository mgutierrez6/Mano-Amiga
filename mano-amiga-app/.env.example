# URL de la API (a través del front-service).
# En desarrollo: la IP de tu compu en la red WiFi (ipconfig en Windows) + puerto 8080.
# El celular y la compu tienen que estar en la MISMA red WiFi.
EXPO_PUBLIC_API_URL=http://192.168.56.1:8090/api/v1
